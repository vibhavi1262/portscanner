# 🔐 Port Scanner Project

A simple Python port scanner that checks open ports and shows service names with a progress bar.

## Features
- Port scanning (1–1024)
- Shows open ports
- Displays service names (HTTP, SSH, etc.)
- Progress bar using tqdm

## How to run
```bash
pip install -r requirements.txt
python port_scanner.py

---

